// AO.cpp : 定义控制台应用程序的入口点。
//

#include "ShortPath.h"

int main()
{
	//开始搜索
	Config config;
	config.init();
	CShortPath shortPath;

	shortPath.InitData(&config); //初始化
	shortPath.Search(); //开始搜索
	for(list<string>::iterator it = shortPath.route.begin();it!=shortPath.route.end();it++){
		cout<<*it<<" ";
	}
	cout<<endl;
	return 0;
}

